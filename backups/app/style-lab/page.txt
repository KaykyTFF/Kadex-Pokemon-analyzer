"use client";

import React from "react";
import { useTheme } from "@/components/layout/ThemeProvider";
import { PlusCircle, BarChart2, FolderOpen, Search, Sword, Zap, ShieldAlert } from "lucide-react";
import { TypeBadge } from "@/components/pokemon/TypeBadge";

export default function StyleLab() {
  const { variant, setVariant } = useTheme();

  return (
    <div className="space-y-12 pb-20 animate-in fade-in duration-500">
      <div className="flex flex-col gap-2 border-b border-soft pb-8">
        <h1 className="text-4xl font-black tracking-tighter uppercase italic">
          Style <span className="text-accent-primary">Lab</span>
        </h1>
        <p className="text-text-secondary font-medium">Experiment with visual variants and design tokens.</p>
      </div>

      {/* Variant Selector */}
      <section className="space-y-6">
        <h2 className="text-xs font-bold uppercase tracking-[0.2em] text-text-muted">Select Variant</h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
          <VariantCard 
            title="Blue Core" 
            desc="The standard SaaS-style experience. Clean, efficient, and balanced."
            active={variant === "blue-core"}
            onClick={() => setVariant("blue-core")}
            color="bg-blue-500"
          />
          <VariantCard 
            title="Crimson Tactical" 
            desc="Aggressive competitive identity. High energy and performance focus."
            active={variant === "crimson-tactical"}
            onClick={() => setVariant("crimson-tactical")}
            color="bg-red-500"
          />
          <VariantCard 
            title="Neo Console" 
            desc="High-tech battle station aesthetic. Electric cyan and technical grids."
            active={variant === "neo-console"}
            onClick={() => setVariant("neo-console")}
            color="bg-cyan-400"
          />
        </div>
      </section>

      {/* Component Previews */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-12">
        
        {/* Cards & Buttons */}
        <section className="space-y-8">
          <h2 className="text-xs font-bold uppercase tracking-[0.2em] text-text-muted">Action Components</h2>
          
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div className="bg-card border border-soft rounded-xl p-5 hover:bg-card-hover transition-all group">
              <div className="mb-4 bg-app inline-flex p-3 rounded-lg border border-soft text-accent-primary group-hover:scale-110 transition-transform">
                <Zap size={20} />
              </div>
              <h3 className="font-bold text-sm mb-1 uppercase tracking-tight">Technical Card</h3>
              <p className="text-xs text-text-muted">Standard interaction pattern with accent highlight.</p>
            </div>

            <div className="bg-card border border-soft rounded-xl p-5 flex flex-col justify-between">
              <div>
                <h3 className="font-bold text-sm mb-1">Primary Action</h3>
                <p className="text-xs text-text-muted mb-4">Core system interaction.</p>
              </div>
              <button className="w-full py-2.5 bg-accent-primary text-white font-bold text-xs uppercase tracking-widest rounded-lg shadow-lg shadow-accent-primary/20 hover:bg-accent-secondary transition-all">
                Execute Command
              </button>
            </div>
          </div>

          <div className="p-6 bg-app/50 border border-soft border-dashed rounded-xl flex items-center justify-center gap-4 text-text-muted">
             <PlusCircle size={20} />
             <span className="text-xs font-bold uppercase tracking-widest">Empty State Slot</span>
          </div>
        </section>

        {/* Status & Indicators */}
        <section className="space-y-8">
          <h2 className="text-xs font-bold uppercase tracking-[0.2em] text-text-muted">Data & Status</h2>
          
          <div className="bg-card border border-soft rounded-2xl overflow-hidden shadow-xl">
            <div className="p-5 border-b border-soft bg-app/40 flex justify-between items-center">
               <span className="text-[10px] font-bold uppercase tracking-widest text-text-muted">Analysis Status</span>
               <span className="px-2 py-0.5 bg-status-success/10 text-status-success text-[10px] font-bold rounded border border-status-success/20">STABLE</span>
            </div>
            <div className="p-6 space-y-6">
              <div className="space-y-2">
                <div className="flex justify-between text-[10px] font-mono font-bold uppercase">
                  <span className="text-text-secondary">System Synergy</span>
                  <span className="text-accent-primary">86%</span>
                </div>
                <div className="h-1.5 w-full bg-app rounded-full overflow-hidden">
                  <div className="h-full bg-accent-primary shadow-[0_0_10px_var(--accent-primary)]" style={{ width: '86%' }}></div>
                </div>
              </div>

              <div className="flex gap-2">
                <TypeBadge type="Fire" size="sm" />
                <TypeBadge type="Water" size="sm" />
                <TypeBadge type="Grass" size="sm" />
                <TypeBadge type="Electric" size="sm" />
              </div>
            </div>
          </div>

          <div className="bg-status-danger/5 border border-status-danger/20 p-4 rounded-xl flex gap-4">
            <div className="text-status-danger mt-0.5">
              <ShieldAlert size={18} />
            </div>
            <div>
              <div className="text-xs font-bold uppercase tracking-wider text-status-danger">Alert Directive</div>
              <p className="text-xs text-text-secondary mt-1 leading-relaxed">Critical vulnerability detected in current formation parameters.</p>
            </div>
          </div>
        </section>

      </div>
    </div>
  );
}

function VariantCard({ title, desc, active, onClick, color }: { title: string, desc: string, active: boolean, onClick: () => void, color: string }) {
  return (
    <button 
      onClick={onClick}
      className={`text-left p-6 rounded-2xl border-2 transition-all duration-300 relative overflow-hidden group ${active ? 'bg-card border-accent-primary shadow-2xl shadow-accent-primary/10' : 'bg-app/50 border-soft hover:border-strong'}`}
    >
      <div className={`absolute top-0 right-0 w-24 h-24 ${color} opacity-5 rounded-full -mr-8 -mt-8 group-hover:opacity-10 transition-opacity`}></div>
      <div className="flex items-center gap-3 mb-4">
        <div className={`w-3 h-3 rounded-full ${color} ${active ? 'animate-pulse shadow-[0_0_8px_currentColor]' : ''}`}></div>
        <h3 className={`font-black uppercase italic tracking-tight ${active ? 'text-text-primary' : 'text-text-muted'}`}>{title}</h3>
      </div>
      <p className="text-xs text-text-secondary leading-relaxed">{desc}</p>
      
      {active && (
        <div className="absolute bottom-4 right-4 text-accent-primary">
          <Zap size={16} fill="currentColor" />
        </div>
      )}
    </button>
  );
}
